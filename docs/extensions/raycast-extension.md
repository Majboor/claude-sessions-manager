# Raycast extension (design)

TypeScript Raycast command "Claude Sessions":

- List: read ~/ClaudeSessions/**/ *.app, show project sections, status accessories.
- Detail pane: render Resources/preview.png.
- Action: open the launcher bundle (resumes in Terminal); secondary action
  copies `claude --resume <sid>` to clipboard.
